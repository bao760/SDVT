-- 📍 Teleport nhân vật đến tọa độ chỉ định
local player = game.Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hrp = char:WaitForChild("HumanoidRootPart")

-- Toạ độ đích
local targetPos = Vector3.new(1377.12, -603.74, 2338.20)

-- Thực hiện teleport
hrp.CFrame = CFrame.new(targetPos)

-- Thông báo trên màn hình
game.StarterGui:SetCore("SendNotification", {
    Title = "✅ Teleport Thành Công!",
    Text = string.format("Đã dịch chuyển đến: (%.2f, %.2f, %.2f)", targetPos.X, targetPos.Y, targetPos.Z),
    Duration = 5
})

print("📍 Đã Teleport đến:", targetPos)
